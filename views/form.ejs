<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/styles.css" />
    <link rel="stylesheet" href="/styles/header.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('./partials/header.ejs') %>

    <header>
      <h1><%= title %></h1>
    </header>

    <% if (url.includes('genres')) { %> <%- include('./partials/genreForm.ejs')
    %> <% } else if (url.includes('developers')) { %> <%-
    include('./partials/developerForm.ejs') %> <% } else if
    (url.includes('games')) { %> <%- include('./partials/gameForm.ejs') %> <% }
    %>
  </body>
</html>
